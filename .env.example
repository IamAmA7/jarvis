# Jarvis runs entirely in the browser — no build-time or server-side
# environment variables are required.
#
# API keys (OpenAI + Anthropic) are entered by each user through the
# Settings page in the UI and stored in their browser's localStorage.
